import { DisplayButtonDirective } from './display-button.directive';

describe('DisplayButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayButtonDirective();
    expect(directive).toBeTruthy();
  });
});
